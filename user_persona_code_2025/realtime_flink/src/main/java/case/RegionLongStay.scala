package `case`

case class RegionLongStay(
                           regionId: String,
                           count: Long,
                           windowStart: Long,
                           windowEnd: Long
                         )
