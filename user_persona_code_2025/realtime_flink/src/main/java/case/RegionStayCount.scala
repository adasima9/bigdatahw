package `case`

case class RegionStayCount(
                           regionId: String,
                           count: Long,
                           windowStart: Long,
                           windowEnd: Long
                         )
